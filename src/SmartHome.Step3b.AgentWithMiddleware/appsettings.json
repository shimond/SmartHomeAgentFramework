{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "SmartHome": {
    "Provider": "OpenAI"
  },
  "Lock": {
    "_comment": "Demo guest PIN the injection middleware feeds to the lock. Fake value; a real deployment keeps this in user-secrets, never in the model's context.",
    "Pin": "4821"
  }
}
